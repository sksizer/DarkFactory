---
id: PRD-600.3.3
title: "Add --json support to tree command"
kind: task
status: draft
priority: medium
effort: s
capability: moderate
parent: "[[PRD-600.3-operational-hardening]]"
depends_on: []
blocks: []
impacts:
  - src/darkfactory/cli/tree.py
workflow:
assignee:
reviewers: []
target_version:
created: 2026-04-09
updated: '2026-04-11'
tags:
  - cli
  - json
  - feature
---

# Add --json support to tree command

## Summary

`prd tree` displays the containment hierarchy with Rich-formatted tree output but has no `--json` mode. Downstream tools and scripts need the hierarchy as structured data. The `--json` global flag is silently ignored.

## Technical approach

`cmd_tree` (in `src/darkfactory/cli/tree.py`, after the PRD-556 CLI split) currently uses helper functions to render Rich tree output. Add a JSON branch that emits the tree as nested objects:

```json
{
  "id": "PRD-500",
  "title": "Extract harness...",
  "status": "done",
  "kind": "epic",
  "children": [
    {"id": "PRD-501", "title": "Scaffold...", "status": "done", "kind": "task", "children": []},
    ...
  ]
}
```

For `prd tree` (full forest), emit a JSON array of root nodes. For `prd tree PRD-500` (rooted), emit a single root object.

## Acceptance criteria

- [ ] AC-1: `prd tree --json` emits JSON array of root trees
- [ ] AC-2: `prd tree PRD-500 --json` emits single root tree object
- [ ] AC-3: Each node has `id`, `title`, `status`, `kind`, `children` fields
- [ ] AC-4: Human-readable output is unchanged when `--json` is not passed
- [ ] AC-5: Test covers both full-forest and rooted JSON output

## Assessment (2026-04-11)

- **Value**: 3/5 — useful for dashboards, IDE plugins, or scripts
  that want the containment hierarchy without grepping Rich
  output. Lower frequency than validate, but the structure is
  the same kind of data.
- **Effort**: s — `cli/tree.py` uses the existing containment tree
  computation and renders via Rich. The JSON branch is a
  parallel render path, not new data. Grep confirms no `--json`
  consumption in tree.py today.
- **Current state**: greenfield.
- **Gaps**: all five ACs unmet.
- **Recommendation**: do-now — pair with PRD-600.3.2 as a single
  "structured output for read commands" PR.
