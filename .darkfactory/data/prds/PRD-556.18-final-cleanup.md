---
id: "PRD-556.18"
title: "Final cleanup — remove residual cli.py, verify cli/ is sole source"
kind: task
status: done
priority: medium
effort: xs
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.2-move-new]]"
  - "[[PRD-556.3-move-status]]"
  - "[[PRD-556.4-move-cleanup]]"
  - "[[PRD-556.5-move-next]]"
  - "[[PRD-556.6-move-validate]]"
  - "[[PRD-556.7-move-tree]]"
  - "[[PRD-556.8-move-children]]"
  - "[[PRD-556.9-move-orphans]]"
  - "[[PRD-556.10-move-undecomposed]]"
  - "[[PRD-556.11-move-conflicts]]"
  - "[[PRD-556.12-move-list-workflows]]"
  - "[[PRD-556.13-move-assign]]"
  - "[[PRD-556.14-move-normalize]]"
  - "[[PRD-556.15-move-plan]]"
  - "[[PRD-556.16-move-run]]"
  - "[[PRD-556.17-move-reconcile]]"
blocks: []
impacts:
  - python/darkfactory/cli/__init__.py
  - tests/test_cli_run.py
  - tests/test_cli_workflows.py
workflow: null
target_version: null
created: 2026-04-08
updated: '2026-04-11'
tags:
  - refactor
  - cli
---

# Final cleanup — remove residual cli.py, verify cli/ is sole source

## Summary

After all 16 subcommand modules have been extracted, `cli/__init__.py` should contain only re-exports and no `cmd_*` function bodies. This task verifies that state, cleans up any remaining dead code in `__init__.py`, confirms `python/darkfactory/cli.py` (the old module file) no longer exists, and ensures no import site references the old `darkfactory.cli.cli` path.

Also review `tests/test_cli_run.py` and `tests/test_cli_workflows.py` — if unit-y tests were migrated to colocated files by earlier PRDs, remove any duplicates from the top-level test files.

## Requirements

1. Verify `python/darkfactory/cli.py` does not exist (it should have been replaced by the `cli/` package in PRD-556.1).
2. Verify `cli/__init__.py` contains only imports and re-exports — no `cmd_*` function bodies remain.
3. Remove any dead imports, unused helpers, or leftover code from `cli/__init__.py`.
4. Grep the codebase for `darkfactory.cli.cli` — no references should remain.
5. Review `tests/test_cli_run.py` and `tests/test_cli_workflows.py` for duplicate tests that were migrated to colocated files. Remove duplicates.
6. All tests pass. `just test && just lint && just typecheck && just format-check` all clean.

## Technical Approach

1. `grep -r "darkfactory.cli.cli" src/ tests/` — should return nothing.
2. `grep -n "^def cmd_" python/darkfactory/cli/__init__.py` — should return nothing.
3. Clean up `__init__.py` to be a slim re-export hub.
4. Run the full test suite to confirm no regressions.

## Acceptance Criteria

- [ ] AC-1: `python/darkfactory/cli.py` does not exist; `python/darkfactory/cli/` is the sole source.
- [ ] AC-2: `cli/__init__.py` contains only imports and re-exports — no function bodies.
- [ ] AC-3: No reference to `darkfactory.cli.cli` exists in the codebase.
- [ ] AC-4: `tests/test_cli_run.py` and `tests/test_cli_workflows.py` have no duplicate tests.
- [ ] AC-5: All tests pass. `just test && just lint && just typecheck && just format-check` clean.
- [ ] AC-6: `from darkfactory.cli import main` still works (public API preserved).

## Assessment (2026-04-11)

- **Value**: 3/5 — closes out the PRD-556 modularization epic
  cleanly. Low individual value but high meta-value (the epic
  stays blocked from closing while this is open).
- **Effort**: xs — four greps and a quick scan of
  `tests/test_cli_run.py` + `tests/test_cli_workflows.py` for
  duplicate unit tests to delete. State survey already confirms
  `python/darkfactory/cli.py` is gone, so AC-1 is trivially met.
- **Current state**: partially landed. Status is `review`, suggesting
  most of the work was done in the original PR. Needs a close-out
  verification pass.
- **Gaps to fully implement**:
  - Run the four greps from the Technical Approach.
  - Dedupe any remaining tests in the two top-level test files.
  - Final `just test && just lint && just typecheck`.
  - Flip to `done`.
- **Recommendation**: do-now — literally half an hour of verification
  work. Once this lands, flip PRD-556 (the epic) to `done` as well
  per its own assessment.
