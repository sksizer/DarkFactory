---
id: "PRD-556.7"
title: "Move cmd_tree into cli/tree.py with colocated tests"
kind: task
status: done
priority: medium
effort: xs
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - python/darkfactory/cli/tree.py
  - python/darkfactory/cli/tree_test.py
  - python/darkfactory/cli/__init__.py
workflow: null
target_version: null
created: 2026-04-08
updated: '2026-04-09'
tags:
  - refactor
  - cli
---

# Move cmd_tree into cli/tree.py with colocated tests

## Summary

Extract `cmd_tree`, `_format_tree_node`, and `_print_tree` from `cli/__init__.py` into `python/darkfactory/cli/tree.py`. Create colocated `tree_test.py`.

## Requirements

1. Move `cmd_tree` (lines ~632–651), `_format_tree_node` (~598–615), and `_print_tree` (~616–631) into `python/darkfactory/cli/tree.py`.
2. Import shared helpers from `cli._shared`.
3. Create `python/darkfactory/cli/tree_test.py` with tests for tree formatting.
4. Update `cli/__init__.py` to import `cmd_tree` from `.tree` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `python/darkfactory/cli/tree.py`

```python
from __future__ import annotations
import argparse
from darkfactory.cli._shared import _load, _default_prd_dir
from darkfactory.prd import PRD
from darkfactory.style import Element, Styler, resolve_style_config

def _format_tree_node(prd: PRD, styler: Styler) -> str: ...
def _print_tree(prds: dict[str, PRD], ...) -> None: ...
def cmd_tree(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_tree`, `_format_tree_node`, `_print_tree` live in `python/darkfactory/cli/tree.py`.
- [ ] AC-2: `tree_test.py` covers tree formatting logic.
- [ ] AC-3: `uv run prd tree` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
