---
id: PRD-556.4
title: Move cmd_cleanup into cli/cleanup.py with colocated tests
kind: task
status: done
priority: medium
effort: s
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - python/darkfactory/cli/cleanup.py
  - python/darkfactory/cli/cleanup_test.py
  - python/darkfactory/cli/__init__.py
workflow:
target_version:
created: 2026-04-08
updated: 2026-04-09
tags:
  - refactor
  - cli
---

# Move cmd_cleanup into cli/cleanup.py with colocated tests

## Summary

Extract `cmd_cleanup` and its helper functions (`_remove_worktree`, `_find_worktree_for_prd`, `_cleanup_single`, `_cleanup_merged`, `_cleanup_all`) from `cli/__init__.py` into `python/darkfactory/cli/cleanup.py`. Create colocated `cleanup_test.py`.

## Requirements

1. Move `cmd_cleanup` (lines ~418–436) and helpers `_remove_worktree` (~287–300), `_find_worktree_for_prd` (~301–325), `_cleanup_single` (~326–339), `_cleanup_merged` (~340–359), `_cleanup_all` (~360–417) into `python/darkfactory/cli/cleanup.py`.
2. Import shared helpers from `cli._shared` and `darkfactory.checks` as needed.
3. Create `python/darkfactory/cli/cleanup_test.py` with tests for the cleanup helpers.
4. Update `cli/__init__.py` to import `cmd_cleanup` from `.cleanup` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `python/darkfactory/cli/cleanup.py`

```python
from __future__ import annotations
import argparse
from pathlib import Path
from darkfactory.cli._shared import _find_repo_root, _load, _default_prd_dir
from darkfactory.checks import StaleWorktree, find_stale_worktrees, is_safe_to_remove

def _remove_worktree(worktree: StaleWorktree, repo_root: Path) -> None: ...
def _find_worktree_for_prd(prd_id: str, repo_root: Path) -> StaleWorktree | None: ...
def _cleanup_single(prd_id: str, force: bool, repo_root: Path) -> int: ...
def _cleanup_merged(force: bool, repo_root: Path) -> int: ...
def _cleanup_all(force: bool, repo_root: Path) -> int: ...
def cmd_cleanup(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_cleanup` and all its helpers live in `python/darkfactory/cli/cleanup.py`.
- [ ] AC-2: `cleanup_test.py` covers the non-trivial cleanup branches.
- [ ] AC-3: `uv run prd cleanup` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
