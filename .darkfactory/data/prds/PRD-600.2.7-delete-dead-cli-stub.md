---
id: PRD-600.2.7
title: "Delete dead cli.py stub file"
kind: task
status: superseded
priority: low
effort: xs
capability: trivial
parent: "[[PRD-600.2-tooling-and-ci]]"
depends_on: []
blocks: []
impacts:
  - python/darkfactory/cli.py
workflow:
assignee:
reviewers: []
target_version:
created: 2026-04-09
updated: '2026-04-11'
tags:
  - cleanup
---

## Superseded by

`python/darkfactory/cli.py` no longer exists. The PRD-556 CLI split
decomposed it into `python/darkfactory/cli/` (a package) and the stub
file has already been removed from the tree. Nothing to delete.

# Delete dead cli.py stub file

## Summary

`python/darkfactory/cli.py` is a 9-line stub that raises `ImportError` unconditionally. Its docstring says it exists "pending deletion once git rm can be run." Python's import system prefers the `cli/` package directory, so this file is never loaded. It should be deleted.

## Technical approach

```bash
git rm python/darkfactory/cli.py
```

Verify no imports reference `darkfactory.cli` in a way that would hit the file instead of the package (they can't -- Python resolves packages over modules).

## Acceptance criteria

- [ ] AC-1: `python/darkfactory/cli.py` no longer exists
- [ ] AC-2: All tests pass
- [ ] AC-3: `from darkfactory.cli import main` still works (resolves to `cli/__init__.py`)

## Assessment (2026-04-11)

- **Value**: n/a — already done.
- **Effort**: n/a
- **Current state**: drift / done. `python/darkfactory/cli.py` does
  not exist (verified directly via `ls`). The `cli/` package is
  the only thing Python finds. All three ACs are implicitly met.
- **Gaps**: none in code.
- **Recommendation**: supersede — flip to `superseded` in the
  sweep. Possibly landed as part of PRD-556.1 (scaffold the cli/
  package).
