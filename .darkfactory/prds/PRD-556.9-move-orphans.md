---
id: "PRD-556.9"
title: "Move cmd_orphans into cli/orphans.py with colocated tests"
kind: task
status: review
priority: medium
effort: xs
capability: trivial
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - src/darkfactory/cli/orphans.py
  - src/darkfactory/cli/orphans_test.py
  - src/darkfactory/cli/__init__.py
workflow: null
target_version: null
created: 2026-04-08
updated: '2026-04-09'
tags:
  - refactor
  - cli
---

# Move cmd_orphans into cli/orphans.py with colocated tests

## Summary

Extract `cmd_orphans` (~7 lines) from `cli/__init__.py` into `src/darkfactory/cli/orphans.py`. Create colocated `orphans_test.py`.

## Requirements

1. Move `cmd_orphans` (lines ~666–673) into `src/darkfactory/cli/orphans.py`.
2. Import shared helpers from `cli._shared`.
3. Create `src/darkfactory/cli/orphans_test.py` with basic tests.
4. Update `cli/__init__.py` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `src/darkfactory/cli/orphans.py`

```python
from __future__ import annotations
import argparse
from darkfactory.cli._shared import _load, _default_prd_dir
from darkfactory.prd import PRD

def cmd_orphans(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_orphans` lives in `src/darkfactory/cli/orphans.py`.
- [ ] AC-2: `orphans_test.py` has basic coverage.
- [ ] AC-3: `uv run prd orphans` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
