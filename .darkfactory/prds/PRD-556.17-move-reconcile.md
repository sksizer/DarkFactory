---
id: "PRD-556.17"
title: "Move cmd_reconcile into cli/reconcile.py with colocated tests"
kind: task
status: ready
priority: medium
effort: s
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - src/darkfactory/cli/reconcile.py
  - src/darkfactory/cli/reconcile_test.py
  - src/darkfactory/cli/__init__.py
workflow: null
target_version: null
created: 2026-04-08
updated: 2026-04-08
tags:
  - refactor
  - cli
---

# Move cmd_reconcile into cli/reconcile.py with colocated tests

## Summary

Extract `cmd_reconcile` and its helpers (`_get_merged_prd_prs`, `_find_prd_file_for_branch`, `_get_prd_status`, `_extract_prd_id_from_path`, `_build_reconcile_commit_msg`, `_commit_to_main`, `_create_reconcile_pr`) from `cli/__init__.py` into `src/darkfactory/cli/reconcile.py`. Create colocated `reconcile_test.py`.

## Requirements

1. Move `cmd_reconcile` (~1434–1479) and helpers `_get_merged_prd_prs` (~1316–1336), `_find_prd_file_for_branch` (~1339–1347), `_get_prd_status` (~1350–1362), `_extract_prd_id_from_path` (~1365–1368), `_build_reconcile_commit_msg` (~1371–1382), `_commit_to_main` (~1385–1398), `_create_reconcile_pr` (~1399–1433) into `src/darkfactory/cli/reconcile.py`.
2. Import shared helpers from `cli._shared` as needed.
3. Create `src/darkfactory/cli/reconcile_test.py` with tests for the pure helpers (`_build_reconcile_commit_msg`, `_extract_prd_id_from_path`, `_find_prd_file_for_branch`).
4. Update `cli/__init__.py` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `src/darkfactory/cli/reconcile.py`

```python
from __future__ import annotations
import argparse
import json
import re
import subprocess
from pathlib import Path
from typing import Any
from darkfactory.cli._shared import _find_repo_root, _load, _default_prd_dir
from darkfactory.prd import PRD, update_frontmatter_field_at

def _get_merged_prd_prs() -> list[dict[str, Any]]: ...
def _find_prd_file_for_branch(branch_name: str, prd_dir: Path) -> Path | None: ...
def _get_prd_status(path: Path) -> str | None: ...
def _extract_prd_id_from_path(prd_file: Path) -> str: ...
def _build_reconcile_commit_msg(candidates: list[tuple[Path, dict[str, Any]]]) -> str: ...
def _commit_to_main(candidates: list[tuple[Path, dict[str, Any]]], repo_root: Path) -> None: ...
def _create_reconcile_pr(candidates: list[tuple[Path, dict[str, Any]]], repo_root: Path) -> None: ...
def cmd_reconcile(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_reconcile` and all its helpers live in `src/darkfactory/cli/reconcile.py`.
- [ ] AC-2: `reconcile_test.py` covers the pure helper logic.
- [ ] AC-3: `uv run prd reconcile` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
