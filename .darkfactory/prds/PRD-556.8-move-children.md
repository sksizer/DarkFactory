---
id: PRD-556.8
title: Move cmd_children into cli/children.py with colocated tests
kind: task
status: done
priority: medium
effort: xs
capability: trivial
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - src/darkfactory/cli/children.py
  - src/darkfactory/cli/children_test.py
  - src/darkfactory/cli/__init__.py
workflow:
target_version:
created: 2026-04-08
updated: '2026-04-10'
tags:
  - refactor
  - cli
---

# Move cmd_children into cli/children.py with colocated tests

## Summary

Extract `cmd_children` (~12 lines) from `cli/__init__.py` into `src/darkfactory/cli/children.py`. Create colocated `children_test.py`.

## Requirements

1. Move `cmd_children` (lines ~653–665) into `src/darkfactory/cli/children.py`.
2. Import shared helpers from `cli._shared`.
3. Create `src/darkfactory/cli/children_test.py` with basic tests.
4. Update `cli/__init__.py` to import `cmd_children` from `.children` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `src/darkfactory/cli/children.py`

```python
from __future__ import annotations
import argparse
from darkfactory.cli._shared import _load, _default_prd_dir
from darkfactory.prd import PRD

def cmd_children(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_children` lives in `src/darkfactory/cli/children.py`.
- [ ] AC-2: `children_test.py` has basic coverage.
- [ ] AC-3: `uv run prd children` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
