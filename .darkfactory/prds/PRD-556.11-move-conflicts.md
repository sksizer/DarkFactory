---
id: "PRD-556.11"
title: "Move cmd_conflicts into cli/conflicts.py with colocated tests"
kind: task
status: done
priority: medium
effort: xs
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - src/darkfactory/cli/conflicts.py
  - src/darkfactory/cli/conflicts_test.py
  - src/darkfactory/cli/__init__.py
workflow: null
target_version: null
created: 2026-04-08
updated: '2026-04-09'
tags:
  - refactor
  - cli
---

# Move cmd_conflicts into cli/conflicts.py with colocated tests

## Summary

Extract `cmd_conflicts` (~53 lines) from `cli/__init__.py` into `src/darkfactory/cli/conflicts.py`. Create colocated `conflicts_test.py`.

## Requirements

1. Move `cmd_conflicts` (lines ~692–745) into `src/darkfactory/cli/conflicts.py`.
2. Import shared helpers from `cli._shared` and `darkfactory.impacts` as needed.
3. Create `src/darkfactory/cli/conflicts_test.py` with tests for the conflict detection logic.
4. Update `cli/__init__.py` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `src/darkfactory/cli/conflicts.py`

```python
from __future__ import annotations
import argparse
from darkfactory.cli._shared import _load, _default_prd_dir
from darkfactory.prd import PRD
from darkfactory import impacts

def cmd_conflicts(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_conflicts` lives in `src/darkfactory/cli/conflicts.py`.
- [ ] AC-2: `conflicts_test.py` covers the conflict detection logic.
- [ ] AC-3: `uv run prd conflicts` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
