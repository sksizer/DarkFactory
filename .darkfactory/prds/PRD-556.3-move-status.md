---
id: "PRD-556.3"
title: "Move cmd_status into cli/status.py with colocated tests"
kind: task
status: in-progress
priority: medium
effort: xs
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - src/darkfactory/cli/status.py
  - src/darkfactory/cli/status_test.py
  - src/darkfactory/cli/__init__.py
workflow: null
target_version: null
created: 2026-04-08
updated: '2026-04-09'
tags:
  - refactor
  - cli
---

# Move cmd_status into cli/status.py with colocated tests

## Summary

Extract `cmd_status` from `cli/__init__.py` into `src/darkfactory/cli/status.py`. Create colocated `status_test.py`.

## Requirements

1. Move `cmd_status` (lines ~224–286) into `src/darkfactory/cli/status.py`.
2. Import shared helpers (`_load`, `_default_prd_dir`, `_action_sort_key`, `PRIORITY_ORDER`, `EFFORT_ORDER`) from `cli._shared`.
3. Create `src/darkfactory/cli/status_test.py` with tests for output formatting and filtering logic.
4. Update `cli/__init__.py` to import `cmd_status` from `.status` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `src/darkfactory/cli/status.py`

```python
from __future__ import annotations
import argparse
from darkfactory.cli._shared import _load, _default_prd_dir, _action_sort_key, PRIORITY_ORDER, EFFORT_ORDER
from darkfactory.prd import PRD
from darkfactory.style import Styler, resolve_style_config

def cmd_status(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_status` lives in `src/darkfactory/cli/status.py`.
- [ ] AC-2: `status_test.py` covers key branches.
- [ ] AC-3: `uv run prd status` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
