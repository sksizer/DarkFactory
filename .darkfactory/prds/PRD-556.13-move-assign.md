---
id: "PRD-556.13"
title: "Move cmd_assign into cli/assign_cmd.py with colocated tests"
kind: task
status: review
priority: medium
effort: xs
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - src/darkfactory/cli/assign_cmd.py
  - src/darkfactory/cli/assign_cmd_test.py
  - src/darkfactory/cli/__init__.py
workflow: null
target_version: null
created: 2026-04-08
updated: '2026-04-09'
tags:
  - refactor
  - cli
---

# Move cmd_assign into cli/assign_cmd.py with colocated tests

## Summary

Extract `cmd_assign` (~71 lines) from `cli/__init__.py` into `src/darkfactory/cli/assign_cmd.py`. Named `assign_cmd` to avoid collision with the `darkfactory.assign` module. Create colocated `assign_cmd_test.py`.

## Requirements

1. Move `cmd_assign` (lines ~784–855) into `src/darkfactory/cli/assign_cmd.py`.
2. Import shared helpers from `cli._shared` and `darkfactory.assign` as needed.
3. Create `src/darkfactory/cli/assign_cmd_test.py` with tests for the assignment logic.
4. Update `cli/__init__.py` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `src/darkfactory/cli/assign_cmd.py`

```python
from __future__ import annotations
import argparse
from darkfactory.cli._shared import _load, _default_prd_dir, _default_workflows_dir, _load_workflows_or_fail
from darkfactory.prd import PRD, set_workflow
from darkfactory import assign

def cmd_assign(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_assign` lives in `src/darkfactory/cli/assign_cmd.py`.
- [ ] AC-2: `assign_cmd_test.py` covers key branches.
- [ ] AC-3: `uv run prd assign` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
