---
id: "PRD-556.5"
title: "Move cmd_next into cli/next_cmd.py with colocated tests"
kind: task
status: done
priority: medium
effort: xs
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on:
  - "[[PRD-556.1-scaffold-cli-package]]"
blocks:
  - "[[PRD-556.18-final-cleanup]]"
impacts:
  - src/darkfactory/cli/next_cmd.py
  - src/darkfactory/cli/next_cmd_test.py
  - src/darkfactory/cli/__init__.py
workflow: null
target_version: null
created: 2026-04-08
updated: '2026-04-09'
tags:
  - refactor
  - cli
---

# Move cmd_next into cli/next_cmd.py with colocated tests

## Summary

Extract `cmd_next` from `cli/__init__.py` into `src/darkfactory/cli/next_cmd.py`. Named `next_cmd` to avoid collision with Python's `next` builtin. Create colocated `next_cmd_test.py`.

## Requirements

1. Move `cmd_next` (lines ~437–477) into `src/darkfactory/cli/next_cmd.py`.
2. Import shared helpers (`_load`, `_default_prd_dir`, `_action_sort_key`) from `cli._shared`.
3. Create `src/darkfactory/cli/next_cmd_test.py` with tests for the selection logic.
4. Update `cli/__init__.py` to import `cmd_next` from `.next_cmd` and remove the moved code.
5. All existing tests pass.

## Technical Approach

### `src/darkfactory/cli/next_cmd.py`

```python
from __future__ import annotations
import argparse
from darkfactory.cli._shared import _load, _default_prd_dir, _action_sort_key
from darkfactory.prd import PRD

def cmd_next(args: argparse.Namespace) -> int: ...
```

## Acceptance Criteria

- [ ] AC-1: `cmd_next` lives in `src/darkfactory/cli/next_cmd.py`.
- [ ] AC-2: `next_cmd_test.py` covers the selection logic.
- [ ] AC-3: `uv run prd next` behavior is identical.
- [ ] AC-4: All existing tests pass. `just test && just lint && just typecheck && just format-check` clean.
