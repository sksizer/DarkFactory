---
id: PRD-556.1
title: Scaffold cli/ package with __init__, _parser, _shared, and main
kind: task
status: review
priority: medium
effort: s
capability: simple
parent: "[[PRD-556-modularize-cli]]"
depends_on: []
blocks:
  - "[[PRD-556.2-move-new]]"
  - "[[PRD-556.3-move-status]]"
  - "[[PRD-556.4-move-cleanup]]"
  - "[[PRD-556.5-move-next]]"
  - "[[PRD-556.6-move-validate]]"
  - "[[PRD-556.7-move-tree]]"
  - "[[PRD-556.8-move-children]]"
  - "[[PRD-556.9-move-orphans]]"
  - "[[PRD-556.10-move-undecomposed]]"
  - "[[PRD-556.11-move-conflicts]]"
  - "[[PRD-556.12-move-list-workflows]]"
  - "[[PRD-556.13-move-assign]]"
  - "[[PRD-556.14-move-normalize]]"
  - "[[PRD-556.15-move-plan]]"
  - "[[PRD-556.16-move-run]]"
  - "[[PRD-556.17-move-reconcile]]"
impacts:
  - src/darkfactory/cli/__init__.py
  - src/darkfactory/cli/_parser.py
  - src/darkfactory/cli/_shared.py
  - src/darkfactory/cli/_shared_test.py
  - src/darkfactory/cli/main.py
  - src/darkfactory/cli/main_test.py
  - src/darkfactory/cli.py
workflow:
target_version:
created: 2026-04-08
updated: '2026-04-09'
tags:
  - refactor
  - organization
  - cli
---

# Scaffold cli/ package with __init__, _parser, _shared, and main

## Summary

Convert `src/darkfactory/cli.py` from a single module into a Python package by creating the `src/darkfactory/cli/` directory with `__init__.py`, `_parser.py`, `_shared.py`, and `main.py`. Move the shared helpers, `build_parser()`, `_configure_logging()`, `main()`, and the module-level constants into their respective new modules. `__init__.py` re-exports `main` and `build_parser` so all existing import sites continue to work unchanged.

**No subcommand functions are moved in this task.** All `cmd_*` functions remain importable from the old location via `__init__.py` re-exports. This is an empty functional change — all tests pass before and after.

## Requirements

1. Create `src/darkfactory/cli/` as a Python package.
2. Move `_find_repo_root`, `_load`, `_default_prd_dir`, `_default_workflows_dir`, `_load_workflows_or_fail`, `_action_sort_key` into `src/darkfactory/cli/_shared.py`. Also move `PRIORITY_ORDER`, `EFFORT_ORDER`, `CAPABILITY_ORDER`.
3. Create `src/darkfactory/cli/_shared_test.py` with unit tests for the shared helpers (at minimum `_find_repo_root`, `_load`, `_action_sort_key`).
4. Move `build_parser()` into `src/darkfactory/cli/_parser.py`. It imports `cmd_*` functions from `__init__.py` (or directly from submodules once they exist).
5. Move `main(argv)` and `_configure_logging(verbose)` into `src/darkfactory/cli/main.py`.
6. Create `src/darkfactory/cli/main_test.py` with a basic smoke test for the entry point.
7. `src/darkfactory/cli/__init__.py` imports from `_shared`, `_parser`, and `main`, and re-exports the public API (`main`, `build_parser`). It also holds all `cmd_*` functions temporarily (moved verbatim from old file) until later PRDs extract them.
8. Every existing `from darkfactory.cli import X` import site continues to work unchanged.
9. All existing tests pass. `just test`, `just lint`, `just typecheck`, `just format-check` all clean.

## Technical Approach

### `src/darkfactory/cli/_shared.py`

Move from the old `cli.py`:
- `PRIORITY_ORDER`, `EFFORT_ORDER`, `CAPABILITY_ORDER` — module-level dicts
- `_find_repo_root(start: Path) -> Path`
- `_default_prd_dir() -> Path`
- `_default_workflows_dir() -> Path`
- `_load_workflows_or_fail(workflows_dir: Path) -> dict[str, Workflow]`
- `_action_sort_key(prd: PRD) -> tuple[int, int, tuple[int, ...]]`
- `_load(prd_dir: Path) -> dict[str, PRD]`

### `src/darkfactory/cli/_parser.py`

Move `build_parser() -> argparse.ArgumentParser` verbatim. It references `cmd_*` functions as `set_defaults(func=cmd_X)` — these will be imported from the package's `__init__.py` initially.

### `src/darkfactory/cli/main.py`

Move:
- `_configure_logging(verbose: bool) -> None`
- `main(argv: list[str] | None = None) -> int`

### `src/darkfactory/cli/__init__.py`

```python
"""CLI package — re-export hub.

Holds cmd_* functions temporarily until extracted by subsequent PRDs.
Re-exports main and build_parser for backwards compatibility.
"""
from darkfactory.cli._shared import (
    PRIORITY_ORDER, EFFORT_ORDER, CAPABILITY_ORDER,
    _find_repo_root, _default_prd_dir, _default_workflows_dir,
    _load_workflows_or_fail, _action_sort_key, _load,
)
from darkfactory.cli._parser import build_parser
from darkfactory.cli.main import main

# All cmd_* functions live here temporarily...

__all__ = ["main", "build_parser"]
```

### Migration strategy

1. Delete `src/darkfactory/cli.py`.
2. Create `src/darkfactory/cli/` directory.
3. Create `_shared.py`, `_parser.py`, `main.py`, `__init__.py`.
4. `__init__.py` initially holds all `cmd_*` implementations (moved verbatim) plus imports from internal modules.
5. Verify all tests pass — this is a no-op refactor at this point.

## Acceptance Criteria

- [ ] AC-1: `src/darkfactory/cli/` exists as a Python package with `__init__.py`, `_shared.py`, `_parser.py`, and `main.py`.
- [ ] AC-2: `_shared.py` contains all shared helpers and constants listed above.
- [ ] AC-3: `_parser.py` contains `build_parser()`.
- [ ] AC-4: `main.py` contains `main()` and `_configure_logging()`.
- [ ] AC-5: `_shared_test.py` has unit tests for the shared helpers.
- [ ] AC-6: `main_test.py` has a smoke test for the entry point.
- [ ] AC-7: Every existing `from darkfactory.cli import X` import site still resolves.
- [ ] AC-8: All existing tests pass. `just test`, `just lint`, `just typecheck`, `just format-check` all clean.
